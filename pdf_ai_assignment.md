Notes de trucs dont on pourrait parler pour le pdf qu'il faut écrire

utilisé github pour coder ensemble

on a fait une base puis on a implémenté sur une branche chacun de notre côté ce qu'on voulait

base:

champs pour écrire l'api key

pouvoir choisir le modèle utilisé pour le call

puterAvailable ==> si true alors on est en test avec index.html, si false, on est sur ancientbrain

favicon.ico ===> 404 file not found in out local tests, because I don't like errors, I created a favicon

call puter avec cves? => pas trouvé

